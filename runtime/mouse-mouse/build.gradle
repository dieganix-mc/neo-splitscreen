plugins {
    id 'net.fabricmc.fabric-loom' version '1.17.14'
}

def commonJava = "${rootDir}/common/main/java"
def commonResources = "${rootDir}/common/main/resources"

sourceSets {
    main {
        java.srcDirs commonJava, 'fabric/main/java'
        resources.srcDirs commonResources, 'fabric/main/resources'
    }
}

version = mod_version
group = mod_group_id

java.toolchain.languageVersion = JavaLanguageVersion.of(25)

base {
    archivesName = mod_id
}

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    implementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${fabric_api_version}"
}

processResources {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    inputs.property 'version', project.version
    filesMatching('fabric.mod.json') {
        expand version: project.version
    }
}

tasks.named('jar', Jar).configure {
    archiveFileName = "${mod_name}-Fabric-${mod_version}+mc${minecraft_version}.jar"
    destinationDirectory = rootProject.layout.buildDirectory.dir('libs')
    from('LICENSE')
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}


